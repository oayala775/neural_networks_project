# neural_networks_project
This repo contains the Neural Networks final project for the course 2025B.

# How to install the required packages
There's a requirements.txt file included in the file structure, to install all the packages required you must execute the `pip install -r requirements.txt`.

It is advised to use a conda environment or a venv environment to isolate the packages management from other projects on the system.
